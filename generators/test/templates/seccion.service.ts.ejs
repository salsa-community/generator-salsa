import axios from 'axios';

import { I<%= seccionPascalCase%> } from '@/shared/model/msPerfil/<%= seccionDashCase%>.model';

const baseApiUrl = 'services/msperfil/api/campos';

export default class <%= seccionPascalCase%>Service {
  public find(id: string): Promise<I<%= seccionPascalCase%>> {
    return new Promise<I<%= seccionPascalCase%>>((resolve, reject) => {
      axios
        .get(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public retrieve(): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .get(baseApiUrl)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public delete(id: string): Promise<any> {
    return new Promise<any>((resolve, reject) => {
      axios
        .delete(`${baseApiUrl}/${id}`)
        .then(res => {
          resolve(res);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public create(entity: I<%= seccionPascalCase%>): Promise<I<%= seccionPascalCase%>> {
    return new Promise<I<%= seccionPascalCase%>>((resolve, reject) => {
      axios
        .post(`${baseApiUrl}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public update(entity: I<%= seccionPascalCase%>): Promise<I<%= seccionPascalCase%>> {
    return new Promise<I<%= seccion.props.pascalCase%>>((resolve, reject) => {
      axios
        .put(`${baseApiUrl}/${entity}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }

  public partialUpdate(entity: I<%= seccionPascalCase%>): Promise<I<%= seccionPascalCase%>> {
    return new Promise<I<%= seccionPascalCase%>>((resolve, reject) => {
      axios
        .patch(`${baseApiUrl}/${entity}`, entity)
        .then(res => {
          resolve(res.data);
        })
        .catch(err => {
          reject(err);
        });
    });
  }
}
