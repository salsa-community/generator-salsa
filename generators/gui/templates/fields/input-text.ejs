      <%_ let context = locals.context ? locals.context: false; _%>
      <input-text
        id="<%= name.dashCase%>-<% if (context) { %><%= context.name.dashCase %>-<% } %><%= campo.name.dashCase%>-id"
        v-model="$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.$model"
        :label="$t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.label')"
        :description="$t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.description')"
        <%_ if (campo.tooltip) { _%>
        :tooltip="$t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.tooltip')"
        <%_ } _%>
        :readonly="false"
        :required="<%= campo.validations.required %>"
        :valid="$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.$dirty ? !$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.$error : null"
        :validationsCommons="{
          <%_ if (campo.validations.required) { _%>
          requiredValue: !$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.required,
          requiredMessage: $t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.validations.required'),
          <%_ } _%>
          <%_ if (campo.validations.min) { _%>
          minValue: !$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.minLength,
          minMessage: $t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.validations.minMessage', { min: '<%=campo.validations.min%>' }),
          <%_ } _%>
          <%_ if (campo.validations.max) { _%>
          maxValue: !$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.maxLength,
          maxMessage: $t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.validations.maxMessage', { max: '<%=campo.validations.max%>' }),
          <%_ } _%>
          <%_ if (campo.validations.regex) { _%>
          regexValue: !$v.<% if (context) { %><%= context.name.camelCase %>.<% } %><%= campo.name.camelCase%>.<%= campo.constantCase%>,
          regexMessage: $t('<%= pathSubseccion %>.fields.<% if (context) { %><%= context.name.dashCase %>.<% } %><%= campo.name.dashCase%>.validations.regexMessage'),
          <%_ } _%>
        }"
      />