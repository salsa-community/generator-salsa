{
  "<%= page.name.dashCase%>": {
    "title": "<%= page.titleEn%>",
    "subtitle": "<%= page.titleEn%>",
    <%_ let campos = Object.keys(page.properties); _%>
    <%_ campos.forEach((key, idx) => { _%>
    <%_ let campo = page.properties[key]; _%>
    "<%= campo.name.dashCase%>": {
      "label": "<%= campo.titleEn%>",
      "description": "<%= campo.descriptionEn%>",
      "tooltip": "<%= campo.tooltipEn%>",
      "placeHolder": "<%= campo.placeHolderEn%>",
      "validations": {
        "required": "The field <%= campo.titleEn%> is required",
        "minMessage": "The number of characters must be at least of {min}",
        "maxMessage": "The number of characters must be as much as {max}",
        "regexMessage": "Invalid <%= campo.titleEn%> format"
      }<%_ if (campo.type === 'array') { _%>,
        <%_ let items = Object.keys(campo.items.properties); _%>
        <%_ items.forEach((key, idx) => { _%>
          <%_ let item = campo.items.properties[key]; _%>
      "<%= item.name.dashCase%>": {
        "label": "<%= item.titleEn%>",
        "description": "<%= item.descriptionEn%>",
        "tooltip": "<%= item.tooltipEn%>",
        "placeHolder": "<%= item.placeHolderEn%>",
        "validations": {
          "required": "The field <%= item.titleEn%> is required",
          "minMessage": "The number of characters must be at least of {min}",
          "maxMessage": "The number of characters must be as much as {max}",
          "regexMessage": "Invalid <%= item.titleEn%> format"
        }
      }<% if (items.length -1 !== idx ) { %>,<% } %>
        <%_ }) _%>
      <%_ } _%>
      }<% if (campos.length -1 !== idx ) { %>,<% } %>
    <%_ }) _%>
  }
}
